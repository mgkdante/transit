// $lib/components/edge — edge-condition primitives (loading / empty / stale /
// offline / error). Import from here: `import { EdgeState } from '$lib/components/edge'`.

export { default as EdgeState } from './EdgeState.svelte';
