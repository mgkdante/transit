# Seed Notes

Prompt 1 seeds STM provider metadata through the CLI so feed URLs can come from
environment-backed settings.

Fixed SQL seed files can be added later if the project needs repeatable static
reference data beyond the small STM bootstrap rows.
