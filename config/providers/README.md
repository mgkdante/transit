# Provider Configs

This directory stores provider manifests for GTFS / GTFS-RT providers.

Prompt 2 adds `stm.yaml` as the only active V1 manifest. Future providers can
be added by placing additional validated YAML files in this directory.
